# Baseline created from the live schema. On databases that already have these
# tables (they all do, via `db push`), mark it applied without running it:
#   npx prisma migrate resolve --applied 0_init
# New databases: `npx prisma migrate deploy` runs it normally.
